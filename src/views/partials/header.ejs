<!DOCTYPE html>
<html lang="pl">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sklep Internetowy</title>
  <link rel="stylesheet" href="/css/output.css">
  <script>
    if (localStorage.getItem('theme') === 'dark') {
      document.documentElement.classList.add('dark');
    }
  </script>
</head>

<body class="min-h-screen flex flex-col bg-white text-black dark:bg-gray-900 dark:text-white">
  <header class="p-4 bg-white dark:bg-gray-800 shadow flex items-center justify-between">
    <div class="flex items-center gap-6">
      <h1 class="text-xl font-bold">Sklep Internetowy</h1>
      <nav>
        <ul class="flex gap-4">
          <li><a href="/" class="hover:underline">Home</a></li>
          <li><a href="/products" class="hover:underline">Produkty</a></li>
          <% if (user) { %>
            <li><a href="/orders" class="hover:underline">Moje zamówienia</a></li>
            <% if (user.isAdmin) { %>
              <li><a href="/admin" class="hover:underline font-semibold text-purple-600 dark:text-purple-400">Panel
                  Admin</a></li>
              <% } %>
                <% } %>
        </ul>
      </nav>
    </div>

    <div class="flex items-center gap-4">
      <form action="/products/search" method="GET" class="flex">
        <input type="text" name="q" placeholder="Szukaj produktów..." aria-label="Wyszukiwanie produktów"
          class="px-4 py-2 border rounded-l dark:bg-gray-700 dark:border-gray-600 dark:text-white focus:outline-none focus:ring-2 focus:ring-blue-500"
          value="<%= typeof searchQuery !== 'undefined' ? searchQuery : '' %>">
        <button type="submit" aria-label="Szukaj" class="px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-r">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
              d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
          </svg>
        </button>
      </form>

      <% if (user) { %>
        <span class="text-sm">Cześć, <strong>
            <%= user.username %>
          </strong></span>
        <a href="/cart" aria-label="Koszyk" class="hover:opacity-75 transition-opacity">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
              d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z" />
          </svg>
        </a>
        <form method="POST" action="/auth/logout" class="inline">
          <button type="submit" class="text-sm text-red-600 dark:text-red-400 hover:underline">Wyloguj</button>
        </form>
        <% } else { %>
          <a href="/auth/login" class="text-sm hover:underline">Logowanie</a>
          <a href="/auth/register"
            class="text-sm bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded">Rejestracja</a>
          <% } %>

            <label for="darkModeToggle" class="relative inline-flex items-center cursor-pointer">
              <input type="checkbox" id="darkModeToggle" class="sr-only" aria-label="Przełącz tryb ciemny">
              <div
                class="w-12 h-6 bg-gray-300 dark:bg-gray-600 rounded-full shadow-inner transition-colors duration-300">
              </div>
              <div
                class="dot absolute left-1 top-1 w-4 h-4 bg-white rounded-full shadow-md transform transition-transform duration-300">
              </div>
            </label>
    </div>
  </header>

  <main class="flex-1">